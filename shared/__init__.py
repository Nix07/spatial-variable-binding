"""Shared utilities for * sub-experiments.

Used by both `behavioral_analysis` and `last_token_exp`.
Modules here should be experiment-agnostic: anything specific to one
experiment (eval contracts, output schemas, task data) belongs in that
experiment's own subtree.
"""
